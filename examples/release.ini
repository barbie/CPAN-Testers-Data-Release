[MASTER]
logfile=logs/release.log

[CPANSTATS]
driver=mysql
database=cpanstats
dbhost=localhost
dbuser=username
dbpass=password

[BACKUPS]
drivers=<<EOT
SQLite
CSV
EOT

[SQLite]
driver=SQLite
database=data/release.db

[CSV]
driver=CSV
dbfile=data/release.csv
